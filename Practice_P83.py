#练习：尝试爬取东方财富网的数据
#先打开东方财富网的官网：https://www.eastmoney.com/
#在首页导航栏里找到"行情中心"的按钮，点击进入页面，可以看到左边有一个市场或者股票的分类菜单
#我们把光标移动到左侧菜单，找到“沪深京个股”这一项，在它下面选择“上证A股”，再点击“全部上证A股”按钮
# 不过需要注意的是这个列表展示的个股数据并不完整，主要起到一个快速浏览和导航的作用，如果我们想获取完整的股票数据，需要进一步点击个股，进入它的详情页面
#但我们不可能把所有的数据都爬取下来，而是要有针对性地选择核心数据项
#比如股票代码、股票名称、数据更新时间、最新股价、交易日跌涨幅、交易日换手率、总市值、流通市值、交易日成交额、动态市盈率、静态市盈率、滚动市盈率、市净率，以及是否盈利
#此外页面往下滚动可以看到“所属板块”部分，一个股票往往属于多个板块，而一个板块下面也会包含多个股票，所以股票和板块之间个关系应该是多对多的
# 我们还需要把股票所属的“板块名称”一起爬取下来，在后面建立合理的存储关系
#接下来新建一个easymoney文件夹，在里面新建一个爬虫数据项txt，把刚才列出的所有数据项逐条写进去，作为后续编码的参考文档
#接下来我们要判断，这些股票详情页上的数据到底是静态数据还是动态数据
#在浏览器里按下F12或者右键后点击“检查”，打开开发者工具，先点击左上角的Clear按钮，把之前的网络数据清空，然后刷新页面，让浏览器重新抓取所有的网络请求
#接下来点击开发者工具把焦点切换到里面，然后用快捷键ctrl + F打开搜索框，输入页面上的股票名称作为关键字，然后回车进行搜索
#在搜索界面里，第一个出现的是HTML页面，需要先确认一下，这个HTML页面是不是直接包含了我们要爬取的数据，如果是我们就可以直接用xpath去解析
#切换到这个HTML请求的Response标签页，在里面再次按ctrl + F搜索，比如输入一个页面上能看到的数据项名称，
# 比如换手率，虽然能搜到标签位置，但是里面是一个空标签，没有实际数据
#这说明这些数据并不是直接写死在HTML里，而是通过接口动态请求后再填充到页面上的

#继续往下，第二个搜索到的是一个叫ann的接口，返回的数据里全是中文，但是里面的内容其实是页面上的公告，不是我们要找的数据接口
#再往下是Articlelist接口，但它返回的是股票对应贴吧的帖子数据，也就是详情页下面的这个帖子区，所以也不是目标
#下一个是叫get的接口，打开它的Response，这里的数据是JSON格式，而且全是和股票相关的数据型字段，和页面上的数据一对比，发现很多字段是我们要的
    #比如f43字段除以100，就是页面上显示的最新股价，f46除以100，就是今开价，f47除以10000再四舍五入，正好对应成交量
    #f48除以1亿再四舍五入就是成交额，所以我们可以基本确认，这个get接口就是我们后面要用程序请求的数据接口
    #到这里，我们点击开发者工具左上角的停止按钮，避免页面自动刷新导致接口信息别冲掉
    #然后切回到pycharm，在爬虫数据项.txt文档里，把接口的关键信息记录下来
    #接着切回浏览器，选中get请求，点开headers标签页，找到Request URL，复制下来（还可以看到请求方法是GET）
    #然后切回Pycharm，把URL粘贴到文档的“个股数据接口URL”里，以及写明请求方式是GET
    #定位到API后，我们对参数进行一下分析
    #切回到浏览器，选择这个get请求，然后点开payload（负载）选项卡，因为这是一个GET请求，所以它的参数其实都放在了URL的问号后面，在payload（负载）里我们就能清楚看到这些参数
    #对于这些参数，有些名字直观就能看出来含义，也有一些完全猜不出来
    # 像这种情况，我们可以先按照原样用这些参数去请求接口，一般都能拿到数据，
    # 只有在处理分页或者需要特别定制返回结果的时候，我们才需要去尝试修改这些参数，看返回结果的变化，再推测参数的含义
    #invt、fltt、cb这几个光看名字不太好判断是什么，先不管，下面有一个fields，这个看起来很像是应答结果中需要返回的数据字段
    # 也就是说，这个参数决定了接口会返回哪些字段，我们后面确认好需要哪些字段后，可以尝试修改fields的值，让接口只返回我们关心的数据
    #在看secid，应该就是股票代码，我们可以通过修改这个值，来获取不同的股票数据
    #ut不太清楚具体是什么，wbp2u，看名字像是请求的平台标识，比如web或app，剩下的dec以及一些带“-”的参数，看不出来含义，也先不管
    #接下来把这些参数信息也复制到我们的“爬虫数据项.txt”文档里

    #接下来我们要分析接口的返回值：
    #在浏览器中，点开Response选项卡，这个相应结果并不是一个标准的JSON，而是外面包了一层函数调用，也就是说它其实是一个JSONP（JSON with Padding）响应
    #JSONP通常会和请求参数里的callback或cb对应起来，这种格式本来是给浏览器的JavaScript用的
    # 我们在程序里要处理的话，要先把外面的函数名和括号去掉，只留下里面标准的JSON字符串，才能用JSON解析，然后我们就可以逐个分析里面的字段了
    #最上面有一个“rc”字段，可能是result-code的缩写，值为0时表示相应成功
    #data里包含我们真正需要爬取的股票数据，我们一个个对照页面来确认，复制到txt文档里

    #data解析：
    #股票代码和股票名称最容易看出，
    #数据更新时间，页面上显示的是一个时间，但在相应数据里没有直接的年月日，那么可能是以时间戳格式返回的，时间戳一般是10位长度的正整数
        #我们发现f86符合10位整数的特征，复制它的值，在浏览器用时间戳转一下，可以看到转换后得到的时间和页面上显示的一致，说明f86就是“数据更新时间”
    #最新股价，我们切换回浏览器，用Response里的数据和页面数据做比较，看起来f43应该是最新股价，不过它的值比页面上显示的大100倍
    #交易日涨跌幅，应该是f170，同样除以100等于浮点数
    #交易日换手率，应该是f168，同样除以100等于浮点数
    #总市值和流通市值，应该分别是f116和f117，绝大部分个股的总市值和流通市值相等，为了区分它们，我们要找一个总市值和流通市值相差较大的股票，比如有大量限售股的大型国企股，来作为测试样本
        #以中国电信为例子，股票代码是601728，打开开发者工具，输入“中国电信”，找到get接口的Response，找到相应结果的f116和f117，和页面数据作比较，可以确定
        #f116是总市值，f117是流通市值
    #交易日成交额，应该是f48
    #然后是动态市盈率、静态市盈率和滚动市盈率，应该分别是f162、f163、f164
    #市净率应该是f167
    #是否盈利，这种布尔类型的字段通常在接口里是用0或1表示，但是0代表是还是否并没有统一标准，由接口设计者决定，所以我们必须要自己验证
        #在应答接口中确实有一些数据是0或1，但是我们并不能判断哪个字段是盈利字段，所以要在多个股票中去对比判断盈利状态是1还是0去表示，以及是哪个字段去表示
        #经过对比，f288是我们要的字段，是否盈利状态。“0”表示“是”，“1”表示“否”

    #最后是所属板块，在当前get接口里我们找不到板块信息，于是我们在页面上复制一个板块名称，在开发者工具里用Ctrl + F
        #搜索当前股票所处的某一板块，然后我们定位到这个qt/slist/get接口，打开Response选项卡，对比一下响应数据和页面内容
        #可以确定这个接口返回的就是所属板块列表
        #打开headers选项卡，再复制它的URL，以及可以看到请求方式是GET
        #切回到txt，在文档中新增一项内容，先写上标题“所属板块接口URL：”，然后把刚在开发中工具中复制的接口URL粘贴进来，再加上请求方式GET

    #分析接口前整理一下字段：
    #个股详情API有很多字段，其中很多字段已经确认不需要，可以先删掉，这里删掉并不是说接口里就没有这些数据了
        # 而是我们在文档里只保留自己要用的字段，这样后面查阅更清晰，至于完整数据随时都能在浏览器里重新抓包获取

    #接下来看所属板块API的这些参数：
        #将pyaload（负载）选项卡内容复制到txt文档

    #接下来要看接口的返回数据
        #切换回浏览器，点击Response选项卡，全选复制整个响应，然后粘贴到txt里，先观察整体结构
        #rc字段依旧出现了，它的值为0，和个股详情接口一样，表示响应成功
        #data字段包含了我们真正想要的内容
            #在data里面
            #total表示返回的模块总数
            #diff是一个列表，里面存放了每个板块的具体信息
            #重点看diff里的字段：
            #f14可以确认就是板块名称
            #f12看上去是一个编号，应该是板块编号
            #其他字段对我们作用不大，为了避免干扰，我们可以把文档里diff的内容简化掉，只保留一个示例板块的数据

#个股详情页的接口分析已经完成，接下来就要定位并分析股票列表页的API
    #在爬虫数据项.txt文档里，先新增四个标题：股票列表接口URL、请求方式、参数、响应结果，等会把headers的相关信息都填进去
    #切换回浏览器，进入股票列表页面，用开发者工具
    #先点击clear清除掉之前的网络记录，然后刷新页面
    #接下来要定位接口，我们直接用页面上的股票名称做关键字（随便复制一个列表里的股票名称），然后再ctrl + F粘贴股票名称并回车搜索
    #在搜索结果里，有一个接口响应包含了这个股票的名字
    #点开之后，确认下这个接口的Response内容，确实和页面上的股票列表相对应，这个接口就是我们要找的股票列表API
    #为避免页面自动刷新导致接口信息被刷掉，我们点击开发者工具左上角的停止按钮，停止继续抓取新的网络记录（让标志变灰）
    #然后切到headers选项卡，复制这个请求的URL，粘贴到文档对应位置，并标注请求方式为GET
    #接着切到payload选项卡，把参数全部复制出来
        #然后我们还是分析一下参数：参数np、fltt、invt还不能直接确定含义先不管
        #然后是cb，在个股详情接口也有看到，值像是动态生成的callback函数名，具体值对结果是否有影响，需要等postman来测试接口时再做验证
        #参数fs、fid的含义也暂时无法确认
        #参数fields的含义应该仍是获取的数据字段
        #再看pn和pz，它们看起来很像是页码和每页数量
            # 为了验证，切换到浏览器，点开发者工具的记录按钮恢复捕获（就是把前面那个变灰的重新变红），然后在页面上翻到第二页股票列表
            #复制页面上的一个股票名称，粘贴到搜索框中回车搜索，点击搜索结果，打开请求的payload选项卡，可以看到参数pn等于2，说明pn确实是页码
            #切换到txt文档做标注，在参数pz后标注每页数据量
            #至于剩下这些数据，含义暂时不清楚，我们先忽略

    #接着切到Response选项卡，把整个JSON复制下来，粘贴到文档里
        #这个结构和个股详情接口的响应很像，最外层同样有rc，值等于0，说明响应成功
        #在data部分，有一个total字段，表示股票总数
        #下面的diff是一个数组，存放了这一页的股票数据，每个元素就是一只股票，因为里面很多数据，所以我们只保留第一个，其他都删掉
        #再逐个字段和页面上的进行对照
        #f12很明显是股票代码
        #f1、f13、f152这些字段的值比较小，是个位数，看上去不像是价格之类的数据，它们可能是一些分类或类型字段，暂时不能完全确定，但有可能在调用个股详情接口会用到
        #而其他很多字段，像f2、f3、f4，都是股价、涨跌幅之类的行情数据，但我们后续回用详情接口来获取它们，所以在这里可以删掉，不需要重复提取
        #然后我们重点分析f1、f13、f152
            #切换回浏览器，恢复网络请求捕获，点击"深证A股"，复制某个股票名称进行搜索，点击搜索结果跳到Response选项卡
            #这次的响应里，F13的值发生了变化，从1变成了0，对比前后页面的数据，基本可以确定f13就是交易市场类型，我们在txt进行标注
            #至于f1和f152.目前的值没有什么变化，仍然是2，但考虑到他们也可能和市场类型或股票分类相关，继续保留，等后续用postman测试确认

        #现在我们已经把目标页面的分析工作基本完成了，通过开发者工具，我们定位到了核心的动态数据API接口，不仅确认了他们的URL和请求方式，还推断出了一些关键参数和返回字段的含义
        #后续编程时我们就可以直接参考这些标注，不用再反复切换页面去查找，分析阶段的工作到这里告一段落
        #接下来就进入到实战调试接口和编写逻辑代码的环节



#在正式写代码之前，还需要用Postman工具把API调试一遍
#这样做的目的是搞清楚两个问题：1、请求接口时到底需要哪些header请求头；2、有哪些参数值的变化会影响响应结果
#只有把这些问题搞明白了，后面代码才能写的高效

#先来调试个股详情API##################
    #1、创建集合和请求：切换到浏览器，打开任意一个股票详情页，打开开发者工具，搜索股票名找到get接口请求记录
        #对这条请求右键复制cURL（bash),接着切换到Postman，在左侧Collection面板里新建一个空白集合，名字叫东方财富网
        #集合建好后，点击import，把刚才复制的cURL信息粘贴到输入框，点确认以后，Postman会自动生成一个新请求，把这个请求的名字（Request name）改为“个股详情API”
        #并且放到刚才创建的东方财富网集合里，最后点击import into Collection把它导入
    #2、调试参数：请求创建好之后，点击Send按钮测试一下，可以看到相应结果正常返回，说明接口能用
        #接下来我们来修改一下参数，比如把参数cb的值改成“jQuery123",然后再点Send，结果依然正常返回，说明cb参数对响应结果没有什么影响
        #再试试参数secid，把它替换成另外一只股票，比如代码1.601929，点击Send，响应结果也是正常的，而且确实返回的是新股票的数据
            #这就说明后续我们在请求个股详情接口时，只需要修改secid参数，就可以获取不同个股的数据
    #3、调试请求头：Postman切换到headers选项卡，可以看到一大堆请求头，先把他们都取消勾选，再点击Send，结果还是能正常返回
        # 说明调用这个接口的时候，其实不需要额外的请求头，不过处于安全考虑，我们还是带一个User-Agent的头，这样在浏览器看来，我们就是一个正常的浏览器请求
            #所以我们重新勾选User-Agent，发送Send，点击Save保存这个Request
# 到这，个股详情API的调试就完成了

#接下来要调试股票列表API##################
    #1、创建请求：切换到浏览器，打开股票列表页面，找到之前分析过的股票列表接口请求记录（列表中随便选一个股票名进行搜索，注意不要有空格）
        #对这条请求复制为cURL(bash),然后切换到Postman，点击import，把复制的内容粘贴到输入框里，把这个请求的名字改为“股票列表API”，然后同样放在“东方财富网”
        #这个集合里，最后点击Import Into Collection
    #2、调试参数：请求创建完成后，先点击一次Send按钮，可以看到响应结果正常返回，而且返回的就是一串股票列表
        # 这里可以先记一下响应结果里排在前面的股票名称，一会用它来验证参数
    #3、定位交易市场类型参数：在分析阶段，我们已经注意到接口里有很多参数，但当时并不能确定到底哪个参数控制交易市场的类型，要想后面爬取不同市场的股票数据，这个参数必须搞清楚
        #在Postman里查看参数的时候，我们会发现有几个参数值是URL编码过的，像fs、fields和wbp2u，这些看起来不太直观，我们需要先解码一下
            #方法很简单：选中参数值，右键点击Decode URL Component，这样就能看到正常的数据了
            #三个参数都解码完成后，返回浏览器，在股票列表页面，点击其他股票市场选项卡，复制一个页面上股票的名称，搜索后点击对应的接口请求，切换到payload选项卡
            #对比一下它的参数和Postman里的参数，发现只有fs参数的值发生了变化，这说明fs可能是真正决定市场类型的参数
                #我们把浏览器里fs的值复制下来，切换回Postman，把它替换掉原来的值，再点击Send
                    # 可以看到，这次返回的股票列表内容变了，和刚刚选定其他股票列表市场数据完全一致，说明判断没错：fs是交易市场类型的参数
                    #切换到txt文件把这个结论记录下来，并且把上证A股和深证A股的参数值都写下来，这样以后要爬其他市场，只需要找到对应fs值就行
    #4、调试请求头：切换到Postman的headers面板，把所有请求头取消勾选也能成功返回，但是处于保险考虑，仍保留User-Agent，然后Save保存
#到这，股票列表API的调试就完成了

#接下来要调试所属板块API##################
    #1、创建请求：切换到浏览器，打开任意一个股票详情页面，页面左侧会有所属板块信息，我们搜索其中一个板块名称，就能定位到接口请求记录
        #找到后右键这条请求，选择复制为cURL（bash),同样点击在Postman点击Import新建请求，请求名称改为“所属板块API”，同样放在前面那个集合
    #2、调试参数：Send测试成功，响应数据正常
        #修改secid，它的值就是股票代码，我们把它替换成另一个股票，比如1.601929，再点击Send，响应结果依然正常，说明可以通过修改secid来获取不同个股的板块信息
        #然后我们看一下响应结果的结构（对body从JavaScript格式修改为JSON格式），里面有total字段，看起来表示的是总共有多少个板块，但返回的数据里只看到5个板块，我们猜测可能是分页或条数限制的问题
            #检查一下请求参数，可以发现有个参数pz=5，正好等于返回的条数，尝试把pz修改为21，再点击Send，果然看到响应数据里的板块条目变多了，这说明pz就是返回条数的参数
            #后面写代码时我们可以直接把它设置成一个足够大的值，就能一次性拿到完整板块数据
    #3、调试请求头：同样在headers把所有头都取消勾选，再Send，可以看到响应结果依然正常，和前两个一样
#到这里，所属板块API的调试就完成了

#下面给请求API获取数据的主流程代码搭好骨架
    #先创建main.py，然后把要用到的库按用途一一导入























